# Copy this file to `.env` and fill in real values. `.env` is gitignored — never commit it.
#
# Google Stitch MCP server key. Used by .mcp.json via ${STITCH_API_KEY}.
# Get one from Google AI Studio / Cloud console. Rotate immediately if it ever leaks.
STITCH_API_KEY=your-stitch-api-key-here

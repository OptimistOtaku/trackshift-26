# FastF1 downloads and derived frames - large, reproducible, not source
data/cache/
data/frames/
data/*.parquet

# generated outputs. Figures and result tables are small and are the point of the repo, so
# they are committed; logs and cached frames are noise and are not.
artifacts/*.log
artifacts/*.parquet

__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
.build/

# tool caches - regenerated on every run, never source
.pytest_cache/
.ruff_cache/
.mypy_cache/

# AI assistants, IDE configs & scratch
.claude/
.gemini/
.agents/
.chart-data-*/

# Internal hackathon prep, rehearsal scripts & judging cheat sheets
docs/JUDGE_QA.md
docs/FOUR_STAGE_PITCH.md
docs/CATEGORY_ASSESSMENT.md
docs/HACKATHON_READINESS.md
docs/DEMO_CONTRACT.md

# editors
.vscode/
.idea/

# Windows + OneDrive + Office scratch. This repo lives under OneDrive, and opening the
# .pptx deck drops a ~$ lock file next to it - none of it belongs in history.
Thumbs.db
desktop.ini
~$*
*.tmp
